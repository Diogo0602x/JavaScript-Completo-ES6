// Nomeie 3 pro propriedades ou métodos de strings
var nome = 'Diogo';
// fixed
// length
// slice

// Nomeie 5 propriedades ou métodos de elementos do DOM elements
 var btn = document.querySelector('btn');
// addEventListener
// appendChild
// id
// innerHtml
// outerHtml
 console.log(btn)

// Busque no web um objeto (método) capaz de interagir com o clipboard,
// clipboard é a parte do seu computador que lida com o CTRL + C e CTRL + V
new ClipboardJS('.btn');
